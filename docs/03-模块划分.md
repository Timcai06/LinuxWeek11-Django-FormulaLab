# 模块划分

## 推荐目录结构

```text
formula-lab/
  docker-compose.yml
  Dockerfile
  requirements.txt
  .env.example
  manage.py

  config/
    settings/
      base.py
      dev.py
      prod.py
    urls.py
    celery.py

  apps/
    formulas/
      models.py
      urls.py
      tasks.py
      admin.py
      forms.py

      views/
        workbench_views.py
        mission_views.py
        project_views.py
        system_views.py
        shared.py

      selectors/
        missions.py
        projects.py

      presenters/
        missions.py
        projects.py

      services/
        recognizer.py
        ocr_engines.py
        paddle_formula_engine.py
        pix2tex_engine.py
        preprocessing.py
        latex_formats.py
        export_artifacts.py
        health.py
        model_state.py
        telemetry.py

      templates/formulas/
        base.html
        landing.html
        workbench.html
        projects.html
        project_workspace.html
        progress.html
        result.html
        history.html
        system.html

      static/formulas/
        css/
          base.css
          pages/
            landing.css
            workbench.css
            progress.css
            result.css
            result-inspector.css
            result-pipeline.css
            history.css
            system.css
            projects.css
          components/
            console.css
            katex-preview.css
            dashboard.css
            project-workspace.css
            project-workspace-sidebar.css
            project-formula-queue.css
            project-review-drawer.css
            paper-fit-preview.css
            workbench-telemetry.css
        js/
          shared/katex_preview.js
          shared/format_tabs.js
          project_workspace/
            core.js
            tabs.js
            preview.js
            paper_fit.js
            inspector.js
            review_drawer.js
            index.js
          result/
            core.js
            preview.js
            paper_fit.js
            format_controls.js
            theme.js
            copy.js
            image_viewport.js
            index.js
          system/
            core.js
            service_entries.js
            health_render.js
            polling.js
            warmup.js
            index.js
          workbench.js
          progress.js
          history.js
          generated/layout-intelligence.js
  frontend/
    formulas/
      layout_intelligence.js
        visuals/
          landing-mission-bg.png

  docs/
    00-文档索引.md
    01-项目总览.md
    02-总体架构.md
    03-模块划分.md
    ...
```

## config 模块

`config` 是 Django 项目配置层，负责全局配置。第一版采用多文件 settings：

```text
config/settings/base.py
config/settings/dev.py
config/settings/prod.py
```

主要职责：

- 读取环境变量。
- 配置数据库连接。
- 配置 Redis 和 Celery。
- 配置静态文件和媒体文件路径。
- 配置 URL 根路由。

建议把 Celery 初始化放在 `config/celery.py`，让 worker 和 web 使用同一套 Django settings。

## formulas 应用模块

`apps/formulas` 是项目的核心业务应用。

它负责：

- 上传公式图片。
- 创建识别任务。
- 查询任务进度。
- 展示识别结果。
- 展示历史记录。
- 管理公式识别相关的数据模型。

## models.py

`models.py` 定义业务模型。第一版核心模型是 `FormulaJob`，用于表示一次公式图片识别任务。

模型不应包含复杂模型推理逻辑，但可以包含轻量的状态判断方法，例如：

- 是否已完成。
- 是否失败。
- 是否还在运行。
- 计算任务耗时。

## views 目录

`apps/formulas/views/` 负责页面和 HTTP API 编排。项目已经从单个 `views.py` 拆成按页面域划分的模块：

```text
workbench_views.py  上传工作台、创建任务
mission_views.py    进度页、结果页、历史页、重试和 mission API
project_views.py    Projects、Project Workspace、审校和导出
system_views.py     Telemetry 系统状态
shared.py           页面层共享工具
```

它应该做：

- 校验上传表单。
- 保存上传文件。
- 创建任务记录。
- 投递 Celery 任务。
- 返回页面或 JSON。

它不应该做：

- 直接调用 PaddleOCR 或 pix2tex。
- 直接执行耗时推理。
- 处理复杂图片预处理。
- 拼装过厚的页面上下文；复杂查询应下沉到 selectors，展示上下文应下沉到 presenters。

## selectors 与 presenters

`selectors/` 是查询层，负责把页面需要的数据用可复用、可测试的查询表达出来。它不负责 HTML 文案，也不执行写操作。

`presenters/` 是页面展示上下文层，负责把模型对象转换成模板可直接使用的数据结构。它适合承载：

- Project Workspace 的 overview、review data、分页信息。
- Mission Report 的格式化 LaTeX、耗时、状态展示。
- 后续 Quality Dashboard 的指标摘要。

下一轮架构升级应优先把 `project_views.py` 中继续增长的上下文组装逻辑拆入 presenters，避免 view 文件重新变厚。

## tasks.py

`tasks.py` 定义 Celery 后台任务。

它负责：

- 从数据库读取任务。
- 更新任务状态和进度。
- 调用识别服务。
- 保存 LaTeX 结果。
- 捕获异常并写入错误信息。

## services 目录

`services` 是识别与处理能力的封装层。

建议模块：

- `recognizer.py`：统一识别入口，向业务层提供稳定接口。
- `ocr_engines.py`：识别引擎工厂，根据配置选择 PaddleOCR 或 pix2tex。
- `paddle_formula_engine.py`：PaddleOCR Formula Recognition 具体适配器。
- `pix2tex_engine.py`：pix2tex 具体适配器，用于对照和回退。
- `preprocessing.py`：图片格式、尺寸、透明背景等预处理。
- `latex_formats.py`：LaTeX 文本清洗、包裹符号处理和显示修正。
- `export_artifacts.py`：项目级 `.tex` 与 `.md` 即时导出文本生成。
- `health.py`：系统状态检查。
- `model_state.py`：模型状态读写和 UI 状态映射。
- `telemetry.py`：worker heartbeat 等轻量 telemetry 工具。

服务层的目标是隔离第三方模型依赖，让业务层不直接绑定 PaddleOCR 或 pix2tex 的内部 API。

## templates 与 static

模板负责页面结构，静态文件负责样式与交互。

前端资源约定：

- Tailwind CDN 用于基础布局和 utility。
- 本地 CSS 负责 Formula Lab 的 SpaceX-inspired 设计语言。
- CSS 使用 `base.css + pages/*.css`。
- JS 按页面拆分。
- `frontend/formulas/layout_intelligence.js` 是 Pretext 源文件，构建后输出到 `static/formulas/js/generated/layout-intelligence.js`。
- 视觉资产放在 `static/formulas/visuals/`；除 Landing 外，Workbench、Mission Log、Progress、Result、System 全部使用全局细网格背景，不单独引用图片纹理。

第一版页面包括：

- 首页：产品入口。
- Workbench：上传图片，选择 Existing Project 或 New Project。
- Projects：论文项目索引。
- Project Workspace：项目内批次、公式条目、公式 Inspector、Paper Fit Preview、Review Drawer、Workflow Status 和导出入口。
- 进度页：展示任务阶段与进度条。
- 结果页：展示原图、LaTeX 文本、公式渲染、一键复制。
- 历史记录页：展示所有历史识别任务。
- 系统状态页：展示 Web、数据库、Redis、Worker、模型与媒体文件状态。

## 当前架构风险文件

当前已有 governance checker 约束大文件，但仍有一些文件接近继续拆分的临界点。下一轮架构升级应优先处理：

```text
apps/formulas/static/formulas/css/pages/projects.css
apps/formulas/static/formulas/css/pages/result-inspector.css
apps/formulas/static/formulas/css/pages/workbench.css
apps/formulas/views/project_views.py
```

拆分原则不是为了追求文件数量，而是让每个文件围绕一个稳定职责：页面布局、组件样式、review drawer、formula queue、paper fit telemetry、workspace sidebar、export actions、数据查询或页面 presenter。当前 `paper-fit-preview.css`、`project-workspace-sidebar.css`、`static/formulas/js/project_workspace/`、`static/formulas/js/result/` 和 `static/formulas/js/system/` 已经从较大的页面文件中拆出，后续继续按同样方式治理 Workbench 和 CSS 组件。
