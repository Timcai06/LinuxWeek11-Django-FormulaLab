# 模块划分

## 推荐目录结构

```text
formula-lab/
  docker-compose.yml
  Dockerfile
  requirements.txt
  .env.example
  manage.py

  config/
    settings/
      base.py
      dev.py
      prod.py
    urls.py
    celery.py

  apps/
    formulas/
      models.py
      views.py
      urls.py
      tasks.py
      admin.py
      forms.py

      services/
        recognizer.py
        pix2tex_engine.py
        preprocessing.py
        latex_postprocess.py

      templates/formulas/
        首页.html
        进度页.html
        结果页.html
        历史记录.html
        错误页.html

      static/formulas/
        css/
          base.css
          pages/
            landing.css
            workbench.css
            progress.css
            result.css
            history.css
            system.css
        js/
          landing.js
          workbench.js
          progress.js
          result.js
          history.js
          system.js
        visuals/
          landing-mission-bg.png

  docs/
    00-文档索引.md
    01-项目总览.md
    02-总体架构.md
    03-模块划分.md
    ...
```

## config 模块

`config` 是 Django 项目配置层，负责全局配置。第一版采用多文件 settings：

```text
config/settings/base.py
config/settings/dev.py
config/settings/prod.py
```

主要职责：

- 读取环境变量。
- 配置数据库连接。
- 配置 Redis 和 Celery。
- 配置静态文件和媒体文件路径。
- 配置 URL 根路由。

建议把 Celery 初始化放在 `config/celery.py`，让 worker 和 web 使用同一套 Django settings。

## formulas 应用模块

`apps/formulas` 是项目的核心业务应用。

它负责：

- 上传公式图片。
- 创建识别任务。
- 查询任务进度。
- 展示识别结果。
- 展示历史记录。
- 管理公式识别相关的数据模型。

## models.py

`models.py` 定义业务模型。第一版核心模型是 `FormulaJob`，用于表示一次公式图片识别任务。

模型不应包含复杂模型推理逻辑，但可以包含轻量的状态判断方法，例如：

- 是否已完成。
- 是否失败。
- 是否还在运行。
- 计算任务耗时。

## views.py

`views.py` 负责页面和 HTTP API 编排。

它应该做：

- 校验上传表单。
- 保存上传文件。
- 创建任务记录。
- 投递 Celery 任务。
- 返回页面或 JSON。

它不应该做：

- 直接调用 pix2tex。
- 直接执行耗时推理。
- 处理复杂图片预处理。

## tasks.py

`tasks.py` 定义 Celery 后台任务。

它负责：

- 从数据库读取任务。
- 更新任务状态和进度。
- 调用识别服务。
- 保存 LaTeX 结果。
- 捕获异常并写入错误信息。

## services 目录

`services` 是识别与处理能力的封装层。

建议模块：

- `recognizer.py`：统一识别入口，向业务层提供稳定接口。
- `pix2tex_engine.py`：pix2tex 具体适配器。
- `preprocessing.py`：图片格式、尺寸、透明背景等预处理。
- `latex_postprocess.py`：LaTeX 文本清洗、包裹符号处理和显示修正。

服务层的目标是隔离第三方模型依赖，让业务层不直接绑定 pix2tex 的内部 API。

## templates 与 static

模板负责页面结构，静态文件负责样式与交互。

前端资源约定：

- Tailwind CDN 用于基础布局和 utility。
- 本地 CSS 负责 Formula Lab 的 SpaceX-inspired 设计语言。
- CSS 使用 `base.css + pages/*.css`。
- JS 按页面拆分。
- 视觉资产放在 `static/formulas/visuals/`；除 Landing 外，Workbench、Mission Log、Progress、Result、System 全部使用全局细网格背景，不单独引用图片纹理。

第一版页面包括：

- 首页：上传图片，展示产品工作台。
- 进度页：展示任务阶段与进度条。
- 结果页：展示原图、LaTeX 文本、公式渲染、一键复制。
- 历史记录页：展示所有历史识别任务。
- 错误页：展示失败原因和重试入口。
